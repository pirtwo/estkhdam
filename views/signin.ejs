<div class="flex items-center min-h-screen bg-gray-50">
    <div class="flex flex-col rounded shadow-md pt-10 pb-10 px-10 mx-auto bg-purple-600">

        <h1 class="text-center text-2xl text-gray-50 font-black mb-10">
            ورود کاربران
        </h1>

        <div class="rounded shadow relative mb-2 bg-white">
            <div class="absolute inset-y-0 right-0 p-3">
                <i class="fas fa-user-circle"></i>
            </div>
            <input class="w-full rounded p-2 pr-10 focus:outline-none focus:ring-2 focus:ring-green-300 bg-transparent"
                type="text"
                name="email"
                id="email"
                placeholder="آدرس ایمیل">
        </div>

        <div class="rounded shadow relative mb-2 bg-white">
            <div class="absolute inset-y-0 right-0 p-3">
                <i class="fas fa-lock"></i>
            </div>
            <input class="w-full rounded p-2 pr-10 focus:outline-none focus:ring-2 focus:ring-green-300 bg-transparent"
                type="password"
                name="password"
                id="password"
                placeholder="رمز عبور">
        </div>

        <div class="flex mt-5">
            <button type="submit">ورود</button>
            <a href="/">بازگشت</a>
        </div>

    </div>
</div>
